# Frontend (Next.js)

Run:
1. cd frontend
2. npm install
3. npm run dev
Open http://localhost:3000

The frontend expects the backend API at http://localhost:4000/api by default. You can change it in src/utils/api.js
