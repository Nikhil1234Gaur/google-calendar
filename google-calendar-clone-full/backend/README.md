# Backend (Express + Prisma + SQLite)

Run:
1. cd backend
2. npm install
3. npx prisma generate
4. npx prisma db push
5. npm run seed    # to create demo events
6. npm run dev

Server runs on http://localhost:4000 and exposes /api/events endpoints.
